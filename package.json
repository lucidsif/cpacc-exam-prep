{
  "name": "cpacc-test-maker",
  "private": true,
  "version": "0.1.0",
  "description": "CPACC Body of Knowledge practice tests, flashcards, and references.",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "test": "node tests/run.js",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "build": "bash scripts/build-dist.sh",
    "deploy": "npm test && npm run build && wrangler pages deploy dist --project-name=cpacc-test-maker --commit-dirty=true --branch=main",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name=cpacc-test-maker --commit-dirty=true",
    "dev:cf": "npm run build && wrangler pages dev dist"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.62.1",
    "jsdom": "^24.0.0"
  }
}
